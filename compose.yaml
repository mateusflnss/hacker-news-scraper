services:
  api:
    build:
      context: .
    ports:
      - 8000:8000
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=

    command: uvicorn api:app --host 0.0.0.0 --port 8000 --reload


